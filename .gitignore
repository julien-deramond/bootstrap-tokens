node_modules/
*.log
.DS_Store

# `bstokens eject` output — a per-run artifact, not a build product
build/v6-dev/

# `bstokens probe` output — a browser harness, regenerated on demand
build/probe.html

# `bstokens site` output — the deployable copy of the Theme Builder
build/site/
